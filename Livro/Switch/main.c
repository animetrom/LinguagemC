#include <stdio.h>
#include <stdlib.h>

int main() {
	int pri, seg, val;

	printf("Digite o primeiro valor: ");
	scanf_s("%d/n", &pri);
	printf("Digite o segundo valor: ");
	scanf_s("%d", &seg);
	system("cls");
	printf("Digite uma das opções abaixo: /n '/' para divisao/n '+' para soma/n '-' para subtracao/n '*' para multiplicacao/n");

	switch (val) {
		case 
	}

}